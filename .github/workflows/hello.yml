name: Hello

on:
    push:
        branches:
            - master
jobs:
    hello_word:
        runs-on: ubuntu-latest
        steps:
            - name: Check out repository code
              uses: actions/checkout@v4
